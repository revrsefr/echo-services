[package]
name = "echo-helpserv"
version = "0.0.1"
edition = "2021"
description = "HelpServ: a help-desk queue — users ask, operators take and answer."

[dependencies]
echo-api = { path = "../../api" }
