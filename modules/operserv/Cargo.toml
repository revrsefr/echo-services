[package]
name = "echo-operserv"
version = "0.0.1"
edition = "2021"
description = "OperServ: network operator tools, starting with AKILL network bans."

[dependencies]
echo-api = { path = "../../api" }
